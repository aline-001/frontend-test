function countRoles(Users){
    let roles=["admin" , "instructor" ,  "student"];
    let Users= numberOfRoles;

    for (users=0;users<=3; users++){
    let numberOfRoles= CountQueuingStrategy.Users.length;
    alert(Users + numberOfRoles);
    }
    countRoles(Users);
    }

function findAdmins(users){
    if(Users.Roles=="Admin"){
    alert(findAdmins.name);
    } else{
        alert("no admins found");
    }
}

function generateWelcomeMessages(users){
    let i= Users.name;
    let j=Useers.roles;
    if(i.value.length>0&& (j.value.legth>0)){
        alert("Welcome, "+ i + "You are an "+ j + "!");

    }
    else{
        alert("invalid");
    }
}
